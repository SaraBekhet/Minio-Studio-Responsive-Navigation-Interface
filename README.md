# Minio Studio | Responsive Navigation System

A modern, lightweight, and fully responsive navigation interface designed for seamless user experience across all devices.

## 🚀 Overview
**Minio Studio** showcases a clean UI approach to web navigation. The goal was to build a sticky, accessible header that adapts dynamically to different screen sizes without relying on heavy external libraries.

## ✨ Features
* **Fully Responsive:** Seamless transition from desktop links to a mobile hamburger menu.
* **Sticky Header:** Keeps navigation accessible at all times while scrolling.
* **Vanilla JS Toggle:** Lightweight script for handling mobile menu interactions.
* **Modern Styling:** Built with Flexbox, CSS variables, and Google Fonts (Inter).

## 🛠️ Tech Stack
* **HTML5:** Semantic structure.
* **CSS3:** Flexbox, Media Queries, and custom transitions.
* **JavaScript:** Vanilla JS for DOM manipulation.

## 📂 Project Structure
```text
├── css/
│   └── style.css
├── js/
│   └── script.js
└── index.html
